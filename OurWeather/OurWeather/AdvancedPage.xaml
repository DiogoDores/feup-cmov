<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:microcharts="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:custom="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             mc:Ignorable="d"
             x:Class="OurWeather.AdvancedPage"
             NavigationPage.HasNavigationBar="False">

    <CarouselView x:Name="carousel" VerticalOptions="Start" ItemsSource="{Binding Items}"
             HorizontalOptions="FillAndExpand" Margin="0,30,0,20">
        <CarouselView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="32,32,32,128">
                    <custom:PancakeView CornerRadius="32" BackgroundColor="#000000">
                        <StackLayout VerticalOptions="Fill" HorizontalOptions="Fill">
                            <custom:PancakeView VerticalOptions="FillAndExpand" HeightRequest="400" CornerRadius="32"
                                                    BackgroundGradientStartColor="{Binding StartColor}"
                                                    BackgroundGradientEndColor="{Binding EndColor}">
                                <Grid>
                                    <StackLayout Margin="0, 30, 0, 0">
                                        <Label  TranslationX="{Binding Position}" Text="{Binding FormattedName}" TextColor="#FFFFFF" Margin="30,0,0,0" FontSize="26" FontAttributes="Bold" LineBreakMode="NoWrap" />
                                        <Label Text="{Binding Temperature, StringFormat='{0}º'}" HorizontalOptions="Start" TextColor="White" Margin="30,0,0,0" FontSize="50" FontAttributes="Bold" LineBreakMode="NoWrap" />
                                        <Label  TranslationX="{Binding Position}" Text="{Binding Weather}" TextColor="#FFFFFF" Margin="30,0,0,0" FontSize="16" LineBreakMode="NoWrap" />
                                    </StackLayout>
                                    <StackLayout>
                                        <Label TranslationX="{Binding Position}"  Margin="0,0,0,50" TextColor="White" Text="{Binding Tip}" FontSize="14" HorizontalOptions="Center" VerticalOptions="EndAndExpand"/>
                                    </StackLayout>
                                </Grid>
                            </custom:PancakeView>
                        </StackLayout>
                    </custom:PancakeView>
                    <Image Source="{Binding ImageSrc}" VerticalOptions="Center" HorizontalOptions="Center" Margin="0,50,0,0" WidthRequest="250" HeightRequest="250"/>
                </Grid>
            </DataTemplate>
        </CarouselView.ItemTemplate>
    </CarouselView>
    <!--<StackLayout>
        <Label Text="{Binding Name}" VerticalOptions="Start" HorizontalOptions="Start" />
        <Label Text="{Binding Temperature}" VerticalOptions="Start" HorizontalOptions="Start" />
        <Label Text="{Binding Weather}" VerticalOptions="Start" HorizontalOptions="Start" />
        <Label Text="{Binding Pressure}" VerticalOptions="Start" HorizontalOptions="Start" />
        <Label Text="{Binding WindSpeed}" VerticalOptions="Start" HorizontalOptions="Start" />
        <Label Text="{Binding Humidity}" VerticalOptions="Start" HorizontalOptions="Start" />

        <StackLayout Orientation="Horizontal">
            <Button Text="Today" HorizontalOptions="Start" BackgroundColor="Transparent" Clicked="OnTodayButtonClick" />
            <Button Text="Tomorrow" HorizontalOptions="Start" BackgroundColor="Transparent" Clicked="OnTomorrowButtonClick" />
        </StackLayout>

        <microcharts:ChartView x:Name="ChartViewToday" HeightRequest="250"/>
        <microcharts:ChartView x:Name="ChartViewTomorrow" HeightRequest="250" IsVisible="False" />
    </StackLayout>-->
</ContentPage>